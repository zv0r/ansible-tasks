---
- name: Install ansible
  ansible.builtin.apt:
    name: ansible
    update_cache: true
    state: present

- name: Install nginx
  ansible.builtin.apt:
    name: nginx
    update_cache: true
    state: present
